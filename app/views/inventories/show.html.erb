<div class="foods-btn">
  <h2><%= @inventory.name %></h2>
  <%= link_to 'Add Food', new_inventory_inventory_food_path(:inventory_id =>  @inventory.id )%>
</div>
<% if @foods.empty? %>
<p>No foods yet. here is the iventory decription though!</p>
<p> <%= @inventory.description %></p>
<% else %>
<table class="table table-hover table-striped table-bordered">
  <thead class="bg-color">
    <tr>
      <th scope="col">Food</th>
      <th scope="col">Quantity</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @foods.each do |f| %>
      <tr>
        <td><%= f.food.food %></td>
        <td><%= f.quantity %></td>
        <td>
        <% if can? :destroy, f %>
          <%= button_to "Delete Food", inventory_path, method: :delete, class:"btn btn-danger"%>
        <% end %>
        </td>
</tr>
    <% end %>
  </tbody>
</table>
<% end %>